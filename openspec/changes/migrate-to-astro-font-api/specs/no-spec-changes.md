# No Specification Changes

This change is a build-system improvement. Font rendering behaviour is unchanged — the same two fonts (Plus Jakarta Sans, Fraunces) are delivered to the same elements. Only the loading mechanism changes (npm imports → Astro 6 Font API).

No new capabilities are introduced. No existing spec requirements are modified, added, or removed.
