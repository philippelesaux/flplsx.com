# No Specification Changes

This change is a pure implementation refactor. All existing `hero-carousel` requirements are preserved unchanged — only the technology used to implement them changes (React → Astro + plain `<script>`).

No new capabilities are introduced. No existing spec requirements are modified, added, or removed.

See `openspec/specs/hero-carousel/spec.md` for the authoritative requirements that continue to govern this feature.
