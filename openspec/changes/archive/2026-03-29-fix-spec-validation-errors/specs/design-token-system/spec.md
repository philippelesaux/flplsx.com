## MODIFIED Purpose

All design values (colors, spacing, typography) are defined as CSS custom properties in a single `tokens.css` file. Components reference tokens — never hardcoded values. Tailwind is not used.
