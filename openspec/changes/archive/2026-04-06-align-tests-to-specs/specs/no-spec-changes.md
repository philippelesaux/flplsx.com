# No spec changes

This change updates test file metadata only (spec reference comments, `describe`/`it` heading wording, and orphaned describe block removal). No user-observable behavior is added, modified, or removed.

No delta specs are required.
